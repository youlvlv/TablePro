#ifndef SQL_H
#define SQL_H

/* Minimal ODBC header stub for build-time compilation.
   The Dameng driver dynamically loads the ODBC driver manager at runtime,
   so only the umbrella header needs to be valid C. */

#endif /* SQL_H */
